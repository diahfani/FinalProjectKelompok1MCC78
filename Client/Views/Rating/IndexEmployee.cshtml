@model IEnumerable<Client.ViewModels.ReportRatingVM>

@{
    ViewData["Title"] = "IndexEmployee";
    Layout = null;
    string fullname = User.Identity.Name;
}

<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>List Rating - Employee</title>

    <link rel="stylesheet" href="~/assets/css/backend-plugin.min.css">
    <link rel="stylesheet" href="~/assets/css/backend.css?v=1.0.0">
    <link rel="stylesheet" href="~/assets/vendor/remixicon/fonts/remixicon.css">

</head>
<body class="  ">
    <!-- Wrapper Start -->
    <div class="wrapper">

        <div class="iq-sidebar  sidebar-default ">
            <div class="iq-sidebar-logo d-flex align-items-center">
                <a asp-controller="Employee" asp-action="Employee" class="header-logo">
                    <img src="~/assets/images/logo.svg" alt="logo">
                    <h3 class="logo-title light-logo">Employee</h3>
                </a>
                <div class="iq-menu-bt-sidebar ml-0">
                    <i class="las la-bars wrapper-menu"></i>
                </div>
            </div>
            <div class="data-scrollbar" data-scroll="1">
                <nav class="iq-sidebar-menu">
                    <ul id="iq-sidebar-toggle" class="iq-menu">
                        <li class="">
                            <a asp-controller="Employee" asp-action="Employee" class="svg-icon">
                                <svg class="svg-icon" width="25" height="25" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                                    <polyline points="9 22 9 12 15 12 15 22"></polyline>
                                </svg>
                                <span class="ml-4">Dashboard</span>
                            </a>
                        </li>
                        <li class="">
                            <a asp-controller="Task" asp-action="IndexEmployee" class="svg-icon">
                                <svg class="svg-icon" width="25" height="25" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2"></path>
                                    <rect x="8" y="2" width="8" height="4" rx="1" ry="1"></rect>
                                </svg>
                                <span class="ml-4">Task</span>
                            </a>
                        </li>

                        <li class="">
                            <a asp-controller="Report" asp-action="IndexEmployee" class="svg-icon">
                                <svg class="svg-icon" width="25" height="25" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"></path>
                                    <polyline points="3.27 6.96 12 12.01 20.73 6.96"></polyline>
                                    <line x1="12" y1="22.08" x2="12" y2="12"></line>
                                </svg>
                                <span class="ml-4">History Report</span>
                            </a>
                        </li>

                        <li class="Active">
                            <a asp-controller="Rating" asp-action="IndexEmployee" class="svg-icon">
                                <svg class="svg-icon" width="25" height="25" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"></path>
                                    <polyline points="3.27 6.96 12 12.01 20.73 6.96"></polyline>
                                    <line x1="12" y1="22.08" x2="12" y2="12"></line>
                                </svg>
                                <span class="ml-4">Feedback Report</span>
                            </a>
                        </li>

                    </ul>
                </nav>

                <div class="pt-5 pb-2"></div>
            </div>
        </div>
        <div class="iq-top-navbar">
            <div class="iq-navbar-custom">
                <nav class="navbar navbar-expand-lg navbar-light p-0">

                    <div class="navbar-breadcrumb">
                        <h5>Dashboard</h5>
                    </div>
                    <div class="d-flex align-items-center">
                        <button class="navbar-toggler" type="button" data-toggle="collapse"
                                data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                                aria-label="Toggle navigation">
                            <i class="ri-menu-3-line"></i>
                        </button>
                        <div class="collapse navbar-collapse" id="navbarSupportedContent">
                            <ul class="navbar-nav ml-auto navbar-list align-items-center">

                                <li class="nav-item nav-icon dropdown caption-content">
                                    <a href="#" class="search-toggle dropdown-toggle  d-flex align-items-center" id="dropdownMenuButton4"
                                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                        <img src="~/assets/images/user/1.jpg" class="img-fluid rounded-circle" alt="user">
                                        <div class="caption ml-3">
                                            <h6 class="mb-0 line-height">@fullname<i class="las la-angle-down ml-2"></i></h6>
                                        </div>
                                    </a>
                                    <ul class="dropdown-menu dropdown-menu-right border-none" aria-labelledby="dropdownMenuButton">

                                        <li class="dropdown-item  d-flex svg-icon ">
                                            <svg class="svg-icon mr-0 text-primary" id="h-05-p" width="20" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                                            </svg>
                                            <a asp-controller="Account" asp-action="Logout">Logout</a>
                                        </li>
                                    </ul>
                                </li>
                            </ul>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
        <div class="content-page">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex flex-wrap align-items-center justify-content-between breadcrumb-content">
                                    <h5>List Rating</h5>
                                    @*<div class="d-flex flex-wrap align-items-center">
                                    <a href="@Url.Action("Creates", "Task")" class="btn btn-primary">New Task</a>
                                    </div>*@
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-12">
                        <div class="card">

                            <div class="card-body">
                                <div class="row">

                                    <div class="col-lg-12">
                                        @foreach (var item in Model)
                                        {
                                            <div class="card card-widget task-card">
                                                <div class="card-body">
                                                    <div class="d-flex flex-wrap align-items-center justify-content-between">

                                                        @if (item.Report != null)
                                                        {
                                                            <div class="d-flex align-items-center">
                                                                <div>
                                                                    <h5 class="mb-2">@item.Report.Subject</h5>
                                                                </div>
                                                            </div>
                                                            <div class="d-flex align-items-center">
                                                                <div>
                                                                    <p class="mb-0"><i class="las la-calendar-check mr-2"></i>@item.Report.CreatedDate</p>
                                                                </div>
                                                            </div>

                                                        }
                                                        @if (item.Rating != null)
                                                        {
                                                            <div class="flex align-items-center list-user-action">
                                                                <a data-toggle="modal" data-target="#ratingModal_@item.Rating.Guid" class="btn bg-info-light">
                                                                    Rating
                                                                </a>
                                                            </div>
                                                        }
                                                        else
                                                        {
                                                            <div class="flex align-items-center list-user-action">
                                                                <button class="btn btn-outline-light-gray" disabled>
                                                                    Rating
                                                                </button>
                                                            </div>
                                                        }

                                                    </div>
                                                </div>
                                            </div>
                                        }
                                    </div>



                                </div>
                            </div>


                        </div>
                    </div>

                </div>

                <!-- Page end  -->
            </div>
        </div>
    </div>

    @foreach (var item in Model)
    {
        @if(item.Rating != null)
        {
            <div class="modal fade" id="ratingModal_@item.Rating.Guid" tabindex="-1" role="dialog" aria-labelledby="ratingModalLabel_@item.Rating.Guid" aria-hidden="true">
                <div class="modal-dialog" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="ratingModalLabel_@item.Rating.Guid">Rating</h5>
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                        <div class="modal-body">
                            <form asp-action="Index" method="get">
                                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                                <input type="hidden" name="guid" value="@item.Rating.Guid" />
                                <input type="hidden" name="ratingValue" value="@item.Rating.RatingValue" />

                                <div class="rating-stars" id="ratingStars_@item.Rating.Guid" value="@item.Rating.RatingValue">
                                    @for (int i = 1; i <= 5; i++)
                                    {
                                        if (i <= item.Rating.RatingValue)
                                        {
                                            <span class="fa fa-star checked" onclick="updateRating('@item.Rating.Guid', @i)"></span>
                                        }
                                        else
                                        {
                                            <span class="fa fa-star" onclick="updateRating('@item.Rating.Guid', @i)"></span>
                                        }
                                    }
                                </div>



                                <textarea class="custom-textbox" name="comment" readonly>@item.Rating.Comment</textarea>

                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>

                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>

        }
        <!-- Modal -->

    }


    <script>
        $(document).ready(function () {
            $('.modal').on('shown.bs.modal', function () {
                var modalId = $(this).attr('id');
                var ratingId = modalId.replace('ratingModal_', '');
                var ratingValue = $('#ratingValue_' + ratingId).val();
                $('#ratingStars_' + ratingId + ' span').each(function () {
                    if ($(this).data('rating-value') <= ratingValue) {
                        $(this).addClass('checked');
                    } else {
                        $(this).removeClass('checked');
                    }
                });
            });

            $('.modal form').submit(function (event) {
                event.preventDefault(); // Prevent form submission

                var modalId = $(this).closest('.modal').attr('id');
                var ratingId = modalId.replace('ratingModal_', '');
                var ratingValue = 0;

                // Determine the selected rating value
                $('#ratingStars_' + ratingId + ' span').each(function () {
                    if ($(this).hasClass('checked')) {
                        ratingValue = $(this).data('rating-value');
                    }
                });

                // Get the comment value
                var comment = $('#comment_' + ratingId).val();

                // Set the values in the hidden input fields
                $('#ratingValue_' + ratingId).val(ratingValue);

                // Submit the form
                $(this).off('submit').submit();
            });
        });

        function updateRating(guid, rating) {
            var ratingStars = $('#ratingStars_' + guid + ' span');
            ratingStars.each(function (index) {
                if (index < rating) {
                    $(this).addClass('checked');
                } else {
                    $(this).removeClass('checked');
                }
            });

            // Mengatur nilai rating pada atribut data-rating-value
            $('#ratingStars_' + guid).data('rating-value', rating);
        }
    </script>

    <!-- Wrapper End-->
    <!-- Backend Bundle JavaScript -->
    <script src="~/assets/js/backend-bundle.min.js"></script>


    <!-- Chart Custom JavaScript -->
    <script async src="~/assets/js/chart-custom.js"></script>

    <!-- app JavaScript -->
    <script src="~/assets/js/app.js"></script>

</body>
</html>


@*<h1>IndexEmployee</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Report.Subject)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Report.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rating.RatingValue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rating.Comment)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Report.Subject)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Report.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rating.RatingValue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rating.Comment)
            </td>
        </tr>
}
    </tbody>
</table>
*@
